#:import utils kivy.utils
#:include market_folder/marketmapview.kv

 
<MarketScreen>:
    FloatLayout:
        GridLayout:
            cols: 1
            pos_hint: {"top": .895, "left": 1}
            size_hint: 1, .795
            id: marketmapview
        GridLayout:
            rows: 1
            size_hint: 1, .1
            pos_hint: {"top": .1, "right": 1}
            ImageButton:
                source: "my_tools/icons/back2.png"
                canvas.before:
                    Color:
                        rgb: app.theme_cls.primary_color if self.state == 'normal' else utils.get_color_from_hex("#6C5B7B")
                    Rectangle:
                        size: self.size
                        pos: self.pos
                on_release:
                    app.change_screen("home_screen", direction='left', mode='pop')